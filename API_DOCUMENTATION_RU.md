# 📰 NewsGuard - Полная документация API и компонентов

## 🎯 Обзор платформы

**NewsGuard** - это современная платформа для публикации и управления новостями с верификацией контента. Платформа состоит из:

- **Backend API** (FastAPI + PostgreSQL)
- **Frontend** (Next.js + TypeScript)
- **Telegram Bot** (aiogram)
- **Система модерации** и верификации контента

### 🏗️ Архитектура системы

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   Frontend      │    │   Backend API   │    │  Telegram Bot   │
│   (Next.js)     │◄──►│   (FastAPI)     │◄──►│   (aiogram)     │
└─────────────────┘    └─────────────────┘    └─────────────────┘
                              │
                              ▼
                       ┌─────────────────┐
                       │   PostgreSQL    │
                       │   Database      │
                       └─────────────────┘
```

---

## 🔌 Backend API - Полная документация

### 📋 Базовые настройки

**Базовый URL:** `http://192.168.0.245:7070/api`  
**Версия API:** v1  
**Документация:** `/docs` (Swagger UI)  
**ReDoc:** `/redoc`

### 🔐 Аутентификация

#### POST `/auth/signup` - Регистрация пользователя

**Описание:** Создание нового аккаунта пользователя

**Тело запроса:**
```json
{
  "username": "string",
  "email": "string",
  "password": "string",
  "first_name": "string",
  "last_name": "string"
}
```

**Ответ:**
```json
{
  "access_token": "jwt_token_here",
  "user": {
    "id": 1,
    "username": "user123",
    "email": "user@example.com",
    "first_name": "Иван",
    "last_name": "Петров",
    "profile_photo": null,
    "is_active": true,
    "is_verified": false,
    "created_at": "2024-01-01T00:00:00Z"
  },
  "is_admin": false
}
```

#### POST `/auth/signin` - Вход в систему

**Описание:** Аутентификация пользователя

**Тело запроса:**
```json
{
  "username": "string", // username или email
  "password": "string"
}
```

**Ответ:** Аналогичен ответу регистрации

#### GET `/auth/me` - Получение информации о текущем пользователе

**Заголовки:** `Authorization: Bearer <token>`

**Ответ:**
```json
{
  "id": 1,
  "username": "user123",
  "email": "user@example.com",
  "first_name": "Иван",
  "last_name": "Петров",
  "profile_photo": null,
  "is_active": true,
  "is_verified": false,
  "created_at": "2024-01-01T00:00:00Z"
}
```

#### PUT `/auth/profile` - Обновление профиля

**Заголовки:** `Authorization: Bearer <token>`

**Тело запроса:**
```json
{
  "first_name": "string",
  "last_name": "string",
  "profile_photo": "string"
}
```

---

### 📰 API статей

#### GET `/articles` - Получение списка статей

**Параметры запроса:**
- `page` (int, по умолчанию: 1) - Номер страницы
- `limit` (int, по умолчанию: 10, макс: 100) - Количество статей на странице
- `category` (array[string], опционально) - Фильтр по категориям (slug)
- `search` (string, опционально) - Поиск по тексту
- `sort` (string, по умолчанию: "created_at") - Сортировка:
  - `created_at` - По дате создания
  - `views_count` - По количеству просмотров
  - `title` - По алфавиту
  - `featured` - Сначала избранные

**Пример запроса:**
```
GET /articles?page=1&limit=10&category=technology&sort=views_count
```

**Ответ:**
```json
{
  "items": [
    {
      "id": 1,
      "title": "Новые технологии в 2024",
      "slug": "new-technologies-2024",
      "summary": "Обзор последних технологических достижений",
      "author": "Иван Петров",
      "status": "published",
      "featured_image": "https://example.com/image.jpg",
      "views_count": 1250,
      "likes_count": 45,
      "dislikes_count": 2,
      "is_featured": true,
      "is_trending": false,
      "is_breaking_news": false,
      "published_at": "2024-01-01T10:00:00Z",
      "created_at": "2024-01-01T09:00:00Z",
      "category": {
        "id": 1,
        "name": "Technology",
        "slug": "technology"
      }
    }
  ],
  "total": 150,
  "page": 1,
  "limit": 10,
  "pages": 15
}
```

#### GET `/articles/{id}` - Получение статьи по ID

**Описание:** Получает полную информацию о статье и увеличивает счетчик просмотров

**Ответ:**
```json
{
  "id": 1,
  "title": "Новые технологии в 2024",
  "slug": "new-technologies-2024",
  "content": "Полный текст статьи...",
  "summary": "Краткое описание статьи",
  "author": "Иван Петров",
  "status": "published",
  "meta_description": "SEO описание",
  "featured_image": "https://example.com/image.jpg",
  "views_count": 1251,
  "likes_count": 45,
  "dislikes_count": 2,
  "shares_count": 12,
  "is_featured": true,
  "is_trending": false,
  "is_breaking_news": false,
  "published_at": "2024-01-01T10:00:00Z",
  "created_at": "2024-01-01T09:00:00Z",
  "updated_at": "2024-01-01T09:30:00Z",
  "category": {
    "id": 1,
    "name": "Technology",
    "slug": "technology"
  }
}
```

#### POST `/articles/{id}/like` - Лайк статьи

**Описание:** Увеличивает счетчик лайков

**Ответ:**
```json
{
  "message": "Article liked! Total likes: 46",
  "success": true
}
```

#### POST `/articles/{id}/dislike` - Дизлайк статьи

**Описание:** Увеличивает счетчик дизлайков

**Ответ:**
```json
{
  "message": "Article disliked! Total dislikes: 3",
  "success": true
}
```

#### POST `/articles/{id}/share` - Поделиться статьей

**Описание:** Увеличивает счетчик репостов

**Ответ:**
```json
{
  "message": "Article shared! Total shares: 13",
  "success": true
}
```

---

### 📂 API категорий

#### GET `/categories` - Получение всех категорий

**Ответ:**
```json
{
  "items": [
    {
      "id": 1,
      "name": "Technology",
      "slug": "technology",
      "description": "Latest tech news and innovations",
      "created_at": "2024-01-01T00:00:00Z",
      "updated_at": "2024-01-01T00:00:00Z",
      "article_count": 25
    }
  ],
  "total": 8
}
```

#### GET `/categories/{slug}` - Получение категории по slug

**Ответ:** Аналогичен элементу из списка категорий

---

### 🔍 Поиск и тренды

#### GET `/search` - Поиск статей

**Параметры запроса:**
- `q` (string, обязательный) - Поисковый запрос (мин. 2 символа)
- `page` (int, по умолчанию: 1) - Номер страницы
- `limit` (int, по умолчанию: 10, макс: 100) - Количество результатов
- `category` (array[string], опционально) - Фильтр по категориям

**Пример запроса:**
```
GET /search?q=искусственный интеллект&category=technology&limit=5
```

**Ответ:**
```json
{
  "query": "искусственный интеллект",
  "results": [
    {
      "id": 5,
      "title": "ИИ в медицине: новые возможности",
      "slug": "ai-medicine-opportunities",
      "summary": "Как искусственный интеллект меняет медицину",
      "author": "Мария Сидорова",
      "featured_image": "https://example.com/ai-medicine.jpg",
      "views_count": 890,
      "published_at": "2024-01-02T14:00:00Z",
      "category": {
        "id": 1,
        "name": "Technology",
        "slug": "technology"
      }
    }
  ],
  "total": 1,
  "page": 1,
  "limit": 5
}
```

#### GET `/trending` - Трендовые статьи

**Параметры запроса:**
- `limit` (int, по умолчанию: 10, макс: 50) - Количество трендовых статей

**Ответ:**
```json
[
  {
    "id": 3,
    "title": "Прорыв в квантовых вычислениях",
    "slug": "quantum-computing-breakthrough",
    "summary": "Новый рекорд в квантовых вычислениях",
    "author": "Алексей Квантов",
    "featured_image": "https://example.com/quantum.jpg",
    "views_count": 2500,
    "likes_count": 120,
    "dislikes_count": 5,
    "published_at": "2024-01-03T08:00:00Z",
    "category": {
      "id": 1,
      "name": "Technology",
      "slug": "technology"
    }
  }
]
```

---

### 📊 Статистика платформы

#### GET `/stats` - Общая статистика

**Ответ:**
```json
{
  "total_articles": 150,
  "total_users": 1250,
  "accuracy_rate": 95.0
}
```

---

### 📧 Подписка на рассылку

#### POST `/newsletter` - Подписка на рассылку

**Тело запроса:**
```json
{
  "email": "user@example.com"
}
```

**Ответ:**
```json
{
  "message": "Successfully subscribed to newsletter",
  "success": true
}
```

---

## 🛠️ Админ API

### 📊 Статистика для администраторов

#### GET `/admin/stats` - Админ статистика

**Заголовки:** `Authorization: Bearer <admin_token>`

**Ответ:**
```json
{
  "total_articles": 150,
  "published_articles": 120,
  "draft_articles": 25,
  "flagged_articles": 5,
  "total_categories": 8,
  "newsletter_subscribers": 450,
  "total_views": 15000,
  "accuracy_rate": 95.0,
  "recent_articles": [
    {
      "id": 10,
      "title": "Последняя статья",
      "slug": "latest-article",
      "summary": "Описание статьи",
      "author": "Автор",
      "status": "published",
      "featured_image": null,
      "views_count": 100,
      "likes_count": 5,
      "dislikes_count": 0,
      "is_featured": false,
      "is_trending": false,
      "is_breaking_news": false,
      "published_at": "2024-01-05T12:00:00Z",
      "created_at": "2024-01-05T11:00:00Z",
      "category": {
        "id": 2,
        "name": "Business",
        "slug": "business"
      }
    }
  ]
}
```

### 📝 Управление статьями

#### GET `/admin/articles` - Список статей для админа

**Параметры запроса:**
- `page` (int, по умолчанию: 1) - Номер страницы
- `limit` (int, по умолчанию: 10) - Количество статей
- `status` (string, опционально) - Фильтр по статусу: `draft`, `published`, `archived`, `flagged`

#### POST `/admin/articles` - Создание статьи

**Тело запроса:**
```json
{
  "title": "Заголовок статьи",
  "content": "Полный текст статьи...",
  "summary": "Краткое описание",
  "author": "Автор статьи",
  "meta_description": "SEO описание",
  "featured_image": "https://example.com/image.jpg",
  "is_featured": true,
  "is_breaking_news": false,
  "category_id": 1,
  "slug": "article-slug",
  "status": "published"
}
```

#### PUT `/admin/articles/{id}` - Обновление статьи

**Тело запроса:** Аналогично созданию, все поля опциональны

#### DELETE `/admin/articles/{id}` - Удаление статьи

**Ответ:**
```json
{
  "message": "Article deleted successfully",
  "success": true
}
```

### 🛡️ Модерация контента

#### GET `/admin/moderation/flagged` - Список заблокированного контента

**Ответ:**
```json
[
  {
    "id": 1,
    "article_id": 5,
    "article_title": "Заголовок статьи",
    "reason": "spam",
    "description": "Описание проблемы",
    "reporter_email": "reporter@example.com",
    "status": "pending",
    "flagged_at": "2024-01-05T10:00:00Z",
    "reviewed_at": null,
    "reviewer_notes": null
  }
]
```

#### POST `/admin/moderation/{id}/approve` - Одобрение контента

**Тело запроса:**
```json
{
  "notes": "Контент соответствует правилам"
}
```

#### POST `/admin/moderation/{id}/reject` - Отклонение контента

**Тело запроса:**
```json
{
  "notes": "Контент нарушает правила сообщества"
}
```

---

## 🎨 Frontend компоненты

### 🏠 Главная страница (`/`)

**Файл:** `frontend/my-app/app/page.tsx`

**Функции:**
- Отображение избранных новостей
- Фильтрация по категориям
- Адаптивный дизайн
- Загрузка данных с API

**Основные компоненты:**
- `NewsCard` - карточка новости
- `CategorySidebar` - боковая панель категорий
- `Filter` - кнопка фильтрации

### 📰 Страница новостей (`/news`)

**Файл:** `frontend/my-app/app/news/page.tsx`

**Функции:**
- Список всех новостей с пагинацией
- Поиск по тексту
- Сортировка (по дате, популярности, избранным)
- Фильтрация по категориям

**Параметры URL:**
- `?category=technology` - фильтр по категории
- `?sort=views_count` - сортировка

### 📄 Страница статьи (`/news/[id]`)

**Файл:** `frontend/my-app/app/news/[id]/page.tsx`

**Функции:**
- Отображение полного текста статьи
- Счетчик просмотров
- Кнопки лайка/дизлайка/поделиться
- Связанные статьи

### 🏷️ Страница категории (`/category/[slug]`)

**Файл:** `frontend/my-app/app/category/[slug]/page.tsx`

**Функции:**
- Статьи конкретной категории
- Описание категории
- Счетчик статей в категории

### 👤 Профиль пользователя (`/profile`)

**Функции:**
- Информация о пользователе
- История просмотров
- Настройки аккаунта

### 🔐 Аутентификация

#### Страница входа (`/signin`)
- Форма входа
- Валидация данных
- Обработка ошибок

#### Страница регистрации (`/signup`)
- Форма регистрации
- Проверка уникальности email/username
- Автоматический вход после регистрации

### 🛠️ Админ панель (`/admin`)

**Функции:**
- Управление статьями
- Статистика платформы
- Модерация контента
- Управление категориями

---

## 🤖 Telegram Bot интеграция

### 📱 Основные функции бота

**Файл:** `aiogram_bot/app.py`

#### Команда `/start`

**Описание:** Запуск бота и получение приветственного сообщения

**Использование:**
```
/start
```

**Ответ:** Приветственное сообщение с инструкциями

#### Просмотр статей через бота

**Функция:** Пользователи могут просматривать новости прямо в Telegram

**Использование:**
```
/start <article_id>
```

**Пример:**
```
/start 123
```

**Ответ:** Полный текст статьи с ID 123

### 🔧 Технические детали

**Токен бота:** `7938757973:AAH5CozQVpho92cMOVn1M4sI4fhkVg8wUgw`

**База данных:** Подключение к PostgreSQL для получения статей

**Архитектура:**
```python
# Основные компоненты
- Bot: Основной объект бота
- Dispatcher: Обработчик сообщений
- Database: Подключение к PostgreSQL
- Handlers: Обработчики команд
```

### 📋 Планируемые функции бота

**В разработке:**
- 📰 Подписка на категории новостей
- 🔔 Уведомления о новых статьях
- 🔍 Поиск по ключевым словам
- 📊 Статистика просмотров
- ⭐ Избранные статьи
- 🏷️ Фильтрация по категориям

**Примеры будущих команд:**
```
/subscribe technology - подписка на категорию "Technology"
/search искусственный интеллект - поиск статей
/trending - трендовые статьи
/favorites - избранные статьи
```

---

## 🚀 Примеры использования

### 📱 Frontend - Получение новостей

```typescript
import { apiClient } from '@/lib/api'

// Получение последних новостей
const getLatestNews = async () => {
  try {
    const response = await apiClient.getArticles({
      page: 1,
      limit: 10,
      sort: 'created_at'
    })
    console.log('Новости:', response.items)
  } catch (error) {
    console.error('Ошибка загрузки:', error)
  }
}

// Поиск по категории
const getTechNews = async () => {
  try {
    const response = await apiClient.getArticles({
      categories: ['technology'],
      limit: 5
    })
    console.log('Технические новости:', response.items)
  } catch (error) {
    console.error('Ошибка загрузки:', error)
  }
}

// Поиск статей
const searchArticles = async (query: string) => {
  try {
    const response = await apiClient.search(query, {
      page: 1,
      limit: 10
    })
    console.log('Результаты поиска:', response.results)
  } catch (error) {
    console.error('Ошибка поиска:', error)
  }
}
```

### 🔐 Аутентификация

```typescript
// Регистрация пользователя
const signUp = async (userData: {
  username: string
  email: string
  password: string
  first_name: string
  last_name: string
}) => {
  try {
    const response = await fetch('/api/auth/signup', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json'
      },
      body: JSON.stringify(userData)
    })
    
    const data = await response.json()
    
    if (data.access_token) {
      // Сохраняем токен
      localStorage.setItem('token', data.access_token)
      console.log('Успешная регистрация:', data.user)
    }
  } catch (error) {
    console.error('Ошибка регистрации:', error)
  }
}

// Вход в систему
const signIn = async (username: string, password: string) => {
  try {
    const response = await fetch('/api/auth/signin', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({ username, password })
    })
    
    const data = await response.json()
    
    if (data.access_token) {
      localStorage.setItem('token', data.access_token)
      console.log('Успешный вход:', data.user)
    }
  } catch (error) {
    console.error('Ошибка входа:', error)
  }
}
```

### 📰 Взаимодействие со статьями

```typescript
// Лайк статьи
const likeArticle = async (articleId: number) => {
  try {
    const response = await apiClient.likeArticle(articleId.toString())
    console.log('Лайк поставлен:', response.message)
  } catch (error) {
    console.error('Ошибка лайка:', error)
  }
}

// Поделиться статьей
const shareArticle = async (articleId: number) => {
  try {
    const response = await apiClient.shareArticle(articleId.toString())
    console.log('Статья поделена:', response.message)
  } catch (error) {
    console.error('Ошибка репоста:', error)
  }
}

// Подписка на рассылку
const subscribeNewsletter = async (email: string) => {
  try {
    const response = await apiClient.subscribeNewsletter(email)
    console.log('Подписка оформлена:', response.message)
  } catch (error) {
    console.error('Ошибка подписки:', error)
  }
}
```

### 🛠️ Админ функции

```typescript
// Создание статьи (требует админ токен)
const createArticle = async (articleData: {
  title: string
  content: string
  summary: string
  author: string
  category_id: number
  status: 'draft' | 'published'
}) => {
  try {
    const token = localStorage.getItem('admin_token')
    const response = await fetch('/api/admin/articles', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${token}`
      },
      body: JSON.stringify(articleData)
    })
    
    const data = await response.json()
    console.log('Статья создана:', data)
  } catch (error) {
    console.error('Ошибка создания статьи:', error)
  }
}

// Получение админ статистики
const getAdminStats = async () => {
  try {
    const token = localStorage.getItem('admin_token')
    const response = await fetch('/api/admin/stats', {
      headers: {
        'Authorization': `Bearer ${token}`
      }
    })
    
    const data = await response.json()
    console.log('Статистика:', data)
  } catch (error) {
    console.error('Ошибка загрузки статистики:', error)
  }
}
```

---

## 🔧 Настройка и развертывание

### 📋 Требования

**Backend:**
- Python 3.8+
- PostgreSQL 12+
- FastAPI
- Tortoise ORM

**Frontend:**
- Node.js 18+
- Next.js 14+
- TypeScript

**Telegram Bot:**
- Python 3.8+
- aiogram 3.x

### 🚀 Запуск проекта

#### 1. Backend API

```bash
# Установка зависимостей
pip install -r requirements.txt

# Настройка базы данных
# Создайте PostgreSQL базу данных

# Запуск сервера
python src/main.py
```

**API будет доступен по адресу:** `http://localhost:7070`

#### 2. Frontend

```bash
# Переход в директорию фронтенда
cd frontend/my-app

# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev
```

**Frontend будет доступен по адресу:** `http://localhost:3000`

#### 3. Telegram Bot

```bash
# Переход в директорию бота
cd aiogram_bot

# Установка зависимостей
pip install aiogram tortoise-orm

# Запуск бота
python app.py
```

### ⚙️ Конфигурация

**Переменные окружения (.env):**

```env
# Database
DATABASE_URL=postgres://postgres:password@localhost:5432/newsguard

# Security
SECRET_KEY=your-secret-key-change-this-in-production

# API
NEXT_PUBLIC_API_URL=http://localhost:7070/api
FRONTEND_URL=http://localhost:3000

# Telegram Bot
TELEGRAM_BOT_TOKEN=your-bot-token-here
```

---

## 📊 Мониторинг и аналитика

### 📈 Статистика платформы

**Доступные метрики:**
- Общее количество статей
- Количество пользователей
- Точность верификации (95%)
- Количество просмотров
- Популярные категории
- Трендовые статьи

### 🔍 Логирование

**Типы логов:**
- API запросы и ответы
- Ошибки аутентификации
- Действия администраторов
- Просмотры статей
- Модерация контента

---

## 🛡️ Безопасность

### 🔐 Аутентификация

- JWT токены для API доступа
- Хеширование паролей (bcrypt)
- Валидация входных данных
- CORS настройки

### 🛡️ Модерация контента

- Система флагов для подозрительного контента
- Админ панель для модерации
- Автоматическая проверка на спам
- Ручная проверка контента

### 🔒 Защита данных

- Валидация всех входных данных
- SQL injection защита (ORM)
- XSS защита
- CSRF токены

---

## 🚀 Планы развития

### 📱 Telegram Bot

**Ближайшие обновления:**
- Полная интеграция с API
- Подписки на категории
- Уведомления о новых статьях
- Интерактивные кнопки
- Поиск и фильтрация

### 🌐 Frontend

**Планируемые функции:**
- Темная/светлая тема
- Мобильная оптимизация
- PWA поддержка
- Офлайн режим
- Push уведомления

### 🔧 Backend

**Улучшения:**
- Кэширование (Redis)
- Rate limiting
- WebSocket для real-time обновлений
- Микросервисная архитектура
- API версионирование

---

## 📞 Поддержка

**Документация API:** `http://localhost:7070/docs`  
**ReDoc:** `http://localhost:7070/redoc`  
**GitHub:** [Ссылка на репозиторий]  
**Email:** support@newsguard.com

---

*Документация обновлена: 2024-01-05*  
*Версия API: 1.0.0*  
*Версия Frontend: 1.0.0*